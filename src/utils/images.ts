/**
 * Central image map. Swap these to real imports once you export the Figma
 * assets into src/assets, e.g.:
 *
 *   import hero from '@/assets/hero.jpg';
 *   export const PHOTOS = { hero, ... };
 *
 * For now every entry is `undefined`, and <Photo/> renders a palette-tinted
 * gradient placeholder so the layout is correct before assets land.
 */
export const PHOTOS: Record<string, string | undefined> = {
  hero: undefined,
  'service-assist': undefined,
  'service-docs': undefined,
  'service-consult': undefined,
  'service-works': undefined,
  'practice-family': undefined,
  'practice-civil': undefined,
  'practice-criminal': undefined,
  'practice-labor': undefined,
  'updates-featured': undefined,
};
